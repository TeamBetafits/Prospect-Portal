EOD Update — May 23, 2026
================================================================================


1. QUICKSTART FORM — LOCALSTORAGE PERSISTENCE
--------------------------------------------------------------------------------

  - Auto-saves form values to localStorage on every field change
  - Restores from localStorage on browser refresh (survives mid-edit refreshes)
  - localStorage takes priority over DB prefill when user is mid-edit
  - File objects stripped before serialization (not JSON-serializable)
  - Disabled in edit mode — DB prefill always wins there


================================================================================


2. QUICKSTART FORM — EDIT AFTER SUBMISSION
--------------------------------------------------------------------------------

  - Submitted/completed QuickStart cards in the dashboard now show a pencil
    icon overlay on hover and an "Edit Submission" button
  - Pencil and button link to the form with ?edit=true
  - Edit mode bypasses the "already submitted" guard and loads existing
    DB data into the form
  - Editing info banner shown at top of form when in edit mode
  - localStorage auto-save skipped in edit mode entirely


================================================================================


3. QUICKSTART MAPPING — SCHEMA FIXES
--------------------------------------------------------------------------------

  - Removed address_1 from locations upsert (column does not exist in DB)
  - Removed headcount from locations upsert (column does not exist in DB)
  - Removed client_data from singletonTables in portal.ts (table does not exist)


================================================================================


4. MISSING PREMIUMS — SUBMIT & APPROVE ROUTES
--------------------------------------------------------------------------------

  - Added /api/missing-premiums/submit — writes user values to _user staging
    columns, marks form Submitted
  - Added /api/missing-premiums/approve (admin only) — promotes _user staging
    columns to main fields, marks form Completed
  - Both routes update intake_assigned_forms status
  - Added upsertProgressStep shared helper for intake_progress_steps upserts


================================================================================


5. QUICKSTART FORM — 50-EDGE-CASE TEST SCRIPT
--------------------------------------------------------------------------------

  - Created scripts/test-quickstart-e2e.ts — 50 tests, 0 failures
  - Covers: normalizeYearToDate, validateQuickStartRequiredFields,
    companies, users, contacts, entities, locations, benefits,
    contribution_strategies, medical_plans, dental_plans, vision_plans,
    documents_and_artifacts (snapshot, uploads, PEO notes)
  - Confirms address_1 and headcount are NOT in locations payload
  - No network required — pure mapping logic


================================================================================


6. VENDOR FEEDBACK — DATA FLOW & SCHEMA ALIGNMENT
--------------------------------------------------------------------------------

  - Updated the field mapping to reflect removed fields so the current
    mapping stays consistent with the latest form structure and data model
  - Added the missing fields to the vendor_feedback table in Supabase to
    support the full vendor feedback payload
  - Completed the vendor feedback form mapping so the form is now wired to
    the correct fields and aligned with the current schema


================================================================================


Branch: fix/quickstart-persistence-edit-mode (pushed to origin)
